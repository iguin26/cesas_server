export const showAllCourses = (req, res) => {
  res.status(200).send({ msg: "show courses page" });
};

export const showOneCourse = (req, res) => {
  res.status(200).send({ msg: "show one course page" });
};
